apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: gitops-lab
  region: us-east-1

vpc:
  nat:
    gateway: Disable

managedNodeGroups:
  - name: ng-small
    instanceType: t3.small
    desiredCapacity: 2
    privateNetworking: false
    volumeSize: 20